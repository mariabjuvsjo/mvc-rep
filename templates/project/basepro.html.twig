<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome to my Project!
			{% endblock %}
		</title>
		<script src="https://kit.fontawesome.com/df03a50fb4.js" crossorigin="anonymous"></script>
		<link rel="stylesheet" href="{{ asset("css/styleproj.css") }}"/>
		<link
		rel="icon" href="#"/>
	{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}

	</head>
	<body>
		<div class="proj-wrapper">
			<nav class="nav-proj">


				<a class="test" href="{{path("home")}}">Back</a>
				<a class="test" href="{{path("project")}}">Home</a>
				<a class="test" href="/{{app.user ? 'logout' : 'login'}}">
					{{ app.user ? 'Logout' : 'Login' }}</a>
				<a class="test" href="{{path("about_proj")}}">About</a>

				<a class="test" href="/{{app.user ? '' : 'app_register'}}">
					{{ app.user ? '' : 'Register' }}</a>


			</nav>
			{% block content %}{% endblock %}

		</div>
		<footer class="footer-proj">
			<div>
				<p>
					&#169; MARIA BJUVSJÖ
				</p>
			</div>
			<div class="social"></div>

		</footer>
	</body>
</html>
